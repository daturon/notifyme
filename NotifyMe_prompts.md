# NotifyMe — промпты для вайбкодинга

Как пользоваться: выполняйте промпты по порядку, каждый — отдельным сообщением агенту (Claude Code, Cursor и т.п.) в том же проекте/репозитории. После каждого шага — проверяйте, что всё собирается и работает, прежде чем идти дальше. Промпты 1–4 — обязательный костяк, 5–6 — два кейса (можно менять порядком или делать один), 7–10 — доводка.

В начале каждой новой сессии агента (если контекст обнулился) приложите файл `NotifyMe_TZ.md` целиком — промпты на него ссылаются.

---

## Промпт 1. Инициализация проекта и деплой на Vercel

```
Прикладываю ТЗ (NotifyMe_TZ.md). Создай базовый каркас проекта:

- PNPM
- Next.js 15 (App Router) + TypeScript
- Tailwind CSS для стилей
- Структура папок: app/, app/api/, lib/, components/
- Настрой ESLint + Prettier
- Добавь .env.example со всеми переменными окружения, которые понадобятся
  по ТЗ (DATABASE_URL, RESEND_API_KEY, CRON_SECRET, APP_PASSWORD — заглушки,
  реальные значения впишу сам)
- Настрой vercel.json (пока без crons — добавим позже)
- Добавь README с инструкцией: как поднять локально (pnpm install, pnpm run dev),
  какие переменные окружения нужны и где их взять (кратко, по одному предложению
  на переменную)
- Убедись, что `pnpm run build` проходит без ошибок

Не добавляй пока никакой бизнес-логики — только рабочий пустой каркас с одной
заглушечной главной страницей "NotifyMe".
```

---

## Промпт 2. База данных и схема

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 5 "Модель данных" — используй его как основу.

Настрой подключение к Postgres (Supabase) через [Prisma ORM ИЛИ Drizzle ORM —
выбери сам, но обоснуй в комментарии в начале schema-файла, почему].

Сделай:
1. Файл схемы БД с таблицами: events, household_tasks, run_log, notification_log
   — точно по структуре из раздела 5 ТЗ (можешь предложить уточнения типов
   полей, но не меняй набор полей без явной причины)
2. Миграцию для применения этой схемы
3. lib/db.ts — синглтон-клиент для подключения к БД
4. Небольшой seed-скрипт (npm run db:seed), который создаёт одно тестовое
   событие каждого типа ('exchange_rate' и 'weather_task') с валидным config,
   чтобы дальше было на чём тестировать UI и API

Не пиши пока API-роуты — только слой данных.
```

---

## Промпт 3. CRUD API для событий + реестр типов триггеров

```
Прикладываю ТЗ (NotifyMe_TZ.md), разделы 4.1 и 6.

Реализуй backend-часть ядра платформы:

1. lib/triggers/registry.ts — реестр типов триггеров. Каждый тип триггера
   реализует единый интерфейс:

   interface TriggerProvider {
     type: string;
     configSchema: ZodSchema;      // валидация config через zod
     check(config: unknown): Promise<{ triggered: boolean; payload: unknown }>;
   }

   Пока зарегистрируй только один тестовый провайдер-заглушку 'noop', который
   всегда возвращает triggered: false — реальные провайдеры (exchange_rate,
   weather_task) добавим в следующих шагах.

2. API-роуты по списку из раздела 6 ТЗ:
   - GET/POST /api/events
   - PUT/DELETE /api/events/:id
   - POST /api/events/:id/test-run — вызывает check() провайдера соответствующего
     типа и возвращает результат, ничего не отправляя и не сохраняя в
     notification_log (но пишет в run_log)
   - GET /api/events/:id/history — отдаёт последние N записей из run_log и
     notification_log по событию

3. Валидация входных данных через zod на всех эндпоинтах, осмысленные коды
   ошибок (400/404/500) и понятные сообщения.

4. Напиши несколько простых тестов (vitest или jest) на CRUD-роуты, используя
   тестовую БД или мок db-клиента.

Не делай пока UI и не трогай cron.
```

---

## Промпт 4. React UI: список и редактирование событий (общий каркас)

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 8.

Реализуй фронтенд для управления событиями поверх API из промпта 3:

1. Страница /events — список событий карточками: название, тип, статус
   (активно/выключено — toggle), дата последней проверки, дата последнего
   уведомления. Кнопка "проверить сейчас" на каждой карточке (дергает
   test-run и показывает результат во всплывающем блоке/модалке, не уходя
   со страницы).

2. Страница /events/new и /events/[id]/edit — форма создания/редактирования.
   Поля name, type (select из реестра типов), recipient_email, is_active.
   Поле config пока делай как обычный textarea с сырым JSON — визуальные
   формы под конкретные типы добавим в промптах 5 и 6, когда типы появятся.

3. Страница /events/[id]/history — таблица логов из /api/events/:id/history.

4. Общий layout с простой навигацией. Не делай сложный дизайн — достаточно
   чистого, аккуратного Tailwind-минимализма, но пусть будет юзабельно с
   телефона тоже (это личное приложение, я буду проверять его и с мобильного).

5. Используй React Query (или SWR) для запросов к API, покажи состояния
   загрузки и ошибок.

Пока не делай экран задач по дому (household_tasks) — это в промпте 5.
```

---

## Промпт 5. Кейс 1: погодные рекомендации по домашним работам

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 4.3.

Реализуй тип триггера 'weather_task':

1. lib/triggers/weatherTask.ts — провайдер, реализующий интерфейс из промпта 3.
   Использует Open-Meteo (бесплатный API, без ключа, https://open-meteo.com) для
   получения прогноза на 5-7 дней по координатам из config. Логика сопоставления
   погодных условий и задач — как описано в разделе 4.3 ТЗ.

2. API для household_tasks: GET/POST /api/tasks, POST /api/tasks/:id/done
   (проставляет last_done_at = сегодня).

3. UI-экран /events/[id]/tasks для событий типа weather_task:
   - список задач (название, периодичность, погодные условия человекочитаемо,
     дата последнего выполнения, "просрочена / в графике")
   - форма добавления/редактирования задачи с понятными полями (не сырой JSON):
     периодичность в днях, условия погоды через простые контролы (например:
     "минимум N дней подряд без дождя", "температура от X до Y °C")
   - кнопка "отметить выполненным" на каждой задаче

4. Заготовь 2-3 готовых шаблона задач (покос травы, фасадные работы) с
   разумными дефолтными условиями — чтобы можно было добавить задачу в один клик
   и подправить детали.

5. Юнит-тесты на логику сопоставления погоды и условий (chек, что провайдер
   правильно решает triggered: true/false на разных наборах входных данных —
   замокай ответ Open-Meteo).

Координаты города/локации для прогноза — сделай полем в config события
(широта/долгота или название населённого пункта с геокодированием, на твой
выбор, но объясни, какой вариант выбрал и почему).
```

---

## Промпт 6. Кейс 2: курс обмена RUB → BYN

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 4.2 и раздел 9 п.1-2.

Я решил источник данных так: [ВПИШИ СЮДА СВОЙ ВЫБОР — например: "буду сам
поддерживать список из 3-5 URL обменных пунктов, парсер работает по этому
списку" ИЛИ "используем агрегатор X, вот пример страницы с курсами: <ссылка>"].

Реализуй тип триггера 'exchange_rate':

1. lib/triggers/exchangeRate.ts — провайдер, который получает текущие курсы
   RUB->BYN по источнику, который я указал выше, находит максимальный (лучший
   для сдачи RUB) курс и сравнивает с порогом из config.

   Если парсишь HTML — вынеси селекторы в отдельный конфиг-файл
   (lib/triggers/exchangeRateSources.ts), чтобы легко чинить, если сайт
   источника поменяет вёрстку. Обработай случай, когда источник недоступен
   или формат страницы изменился — не должен падать весь cron, только это
   событие, с логом ошибки в run_log.

2. UI-форма конфигурации для этого типа (вместо сырого JSON): порог курса,
   список отслеживаемых точек (чекбоксы/мультиселект из доступных источников).

3. Юнит-тесты на парсинг (с замоканным HTML-ответом) и на логику сравнения
   с порогом.

Учти лимит в 10 секунд на выполнение serverless-функции на Vercel Hobby —
если источников несколько, запрашивай их параллельно (Promise.all), а не
последовательно.
```

---

## Промпт 7. Notification Engine и интеграция с Resend

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 4.4.

Реализуй lib/notifications/engine.ts:

1. Функция notifyIfNeeded(event, checkResult):
   - проверяет, не отправлялось ли уже уведомление по этому event сегодня
     (по notification_log)
   - если triggered=true и сегодня ещё не отправляли — формирует письмо и
     отправляет через Resend
   - логирует результат (успех/ошибка) в notification_log
   - при ошибке отправки делает один retry, затем логирует failed и НЕ бросает
     исключение наружу (чтобы не сломать обработку остальных событий)

2. Шаблоны писем (простой HTML, можно через react-email, если это не усложнит
   сборку) отдельно для каждого типа события — используй payload из
   checkResult провайдера. Для weather_task, если сработало несколько задач
   одновременно — объедини их в одно письмо (см. п.4.3 ТЗ).

3. Тестовый режим: возможность отправить тестовое письмо на свой адрес прямо
   из UI (кнопка "отправить тестовое письмо" на странице события), не завязанная
   на реальное срабатывание триггера — просто чтобы проверить, что Resend
   настроен верно.

Используй переменную окружения RESEND_API_KEY (уже есть в .env.example).
```

---

## Промпт 8. Cron-эндпоинт и подключение планировщика

```
Прикладываю ТЗ (NotifyMe_TZ.md), разделы 3 и 6.

1. Создай app/api/cron/run/route.ts:
   - проверяет заголовок Authorization: Bearer с CRON_SECRET, отклоняет запрос
     с 401, если не совпадает
   - выбирает все активные события из БД
   - для каждого вызывает check() соответствующего провайдера из реестра
     (промпт 3), затем notifyIfNeeded() из промпта 7
   - каждое событие обрабатывай в try/catch независимо — падение одного не
     должно прерывать обработку остальных
   - возвращает сводку: сколько событий проверено, сколько сработало, сколько
     писем отправлено, список ошибок (если были)

2. Добавь crons-секцию в vercel.json:
   { "crons": [{ "path": "/api/cron/run", "schedule": "0 5 * * *" }] }
   (5:00-5:59 UTC — впиши свой часовой пояс исходя из того, во сколько по
   местному времени хочешь получать письма, учти, что Vercel Cron всегда в UTC)

3. Добавь в README раздел: как локально протестировать cron-эндпоинт без
   реального ожидания суток (curl-команда с правильным заголовком).

Напомни мне в ответе, что после деплоя нужно проверить в Vercel Dashboard,
что CRON_SECRET совпадает с тем, что Vercel сгенерировал автоматически
(см. раздел 7 ТЗ).
```

---

## Промпт 9. Простая авторизация доступа к приложению

```
Прикладываю ТЗ (NotifyMe_TZ.md), раздел 9 п.4.

Приложение сейчас полностью открыто на *.vercel.app — нужна минимальная защита,
раз оно публично доступно, но при этом однопользовательское:

Реализуй простую защиту по паролю через middleware.ts (Next.js middleware):
- пароль хранится в переменной окружения APP_PASSWORD (уже в .env.example)
- при первом заходе показывается простая форма ввода пароля
- при верном пароле — ставится httpOnly cookie на 30 дней
- API /api/cron/run НЕ должен требовать этот пароль (он защищён отдельно,
  через CRON_SECRET) — исключи его из middleware

Не усложняй (не нужен полноценный auth-провайдер, NextAuth и т.п.) — это
личный проект на одного пользователя.
```

---

## Промпт 10. Финальная проверка и чек-лист деплоя

```
Прикладываю ТЗ (NotifyMe_TZ.md).

Собери итоговый чек-лист перед продакшн-деплоем на Vercel:

1. Пройдись по всем переменным окружения из .env.example — убедись, что
   каждая используется в коде и нигде нет захардкоженных секретов.
2. Проверь, что npm run build проходит чисто, нет TS-ошибок и неиспользуемых
   импортов.
3. Проверь обработку ошибок во всех внешних вызовах (Open-Meteo, парсинг
   курсов, Resend) — нигде не должно быть необработанных promise rejection,
   которые могут уронить весь cron-запуск.
4. Добавь простую страницу /status (или блок на главной), показывающую:
   время последнего запуска cron, сколько событий активно, сколько писем
   отправлено за последние 7 дней — чтобы визуально видеть, что всё живо,
   не заходя в логи Vercel.
5. Обнови README: финальная инструкция деплоя на Vercel (подключение
   репозитория, добавление всех env-переменных в Vercel Dashboard, где взять
   Supabase connection string и Resend API key).
6. Выведи итоговый список того, что осталось сделать вручную после деплоя
   (например: указать домен для отправки в Resend, проверить cron в Vercel
   Dashboard → Settings → Cron Jobs).

Ничего не переписывай — только проверка, мелкие фиксы и документация.
```

---

## Заметки по порядку

- Промпты 5 и 6 независимы друг от друга — можно поменять местами или
  выполнить только один, если хотите для начала один работающий кейс
  end-to-end (это, скорее всего, разумнее — сначала довести один сценарий
  через промпты 1→2→3→4→5(или 6)→7→8→9→10, задеплоить, убедиться, что письма
  реально приходят, и только потом добавлять второй кейс).
- Открытый вопрос из раздела 9 п.1 ТЗ (источник курсов) нужно решить _до_
  промпта 6 — впишите выбранный вариант прямо в текст промпта, как показано
  в примере.
